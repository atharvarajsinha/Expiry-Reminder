[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = tests
# pytest_bootstrap sets the test environment before Django settings load.
addopts = -q --disable-warnings -p pytest_bootstrap
filterwarnings =
    ignore::DeprecationWarning
